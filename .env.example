# Copy to .env.local for local overrides (not committed).
# Staging builds use .env.staging automatically via: npm run build:staging

# Block search indexing and serve a staging robots.txt when true
STAGING=false
